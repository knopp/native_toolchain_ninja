# Run with `dart run ffigen --config ffigen.yaml`.
name: PackageBindings
description: |
  Bindings for `src/package.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/package_bindings_generated.dart'
headers:
  entry-points:
    - 'src/package.h'
  include-directives:
    - 'src/package.h'
ffi-native:
comments:
  style: any
  length: full
