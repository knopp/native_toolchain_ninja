name: package
description: "A new Dart FFI package project."
version: 0.0.1

environment:
  sdk: ^3.12.0-198.0.dev

dependencies:
  code_assets: ^1.0.0
  hooks: ^1.0.0
  logging: ^1.3.0
  native_toolchain_ninja:
    path: ../..

dev_dependencies:
  ffi: ^2.1.4
  ffigen: ^20.1.1
  flutter_lints: ^6.0.0
  test: ^1.28.0
